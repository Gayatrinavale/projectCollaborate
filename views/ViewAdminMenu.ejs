<!DOCTYPE html>
<html lang="en">
<head>
<<<<<<< HEAD
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Menu</title>
 <link rel="stylesheet" href="/css/adminVertical.css">
  <link rel="stylesheet" href="/css/adminNav.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
  <style>
    .main-layout {
  display: flex;
  width: 100%;
}

.adminVertical {  /* If this is your sidebar class */
  width: 300px;
  position: fixed;
  left: 0;
  top: 0;
  bottom: 0;
  z-index: 1000;
}

.content-area {
  margin-left: 300px; /* match the sidebar width */
  width: calc(100% - 300px);
}

.view-menu-container {
  position: relative;
  z-index: 1;
  padding: 100px 20px 20px 20px; /* padding-top gives space for navbar */
  display: flex;
  flex-direction: column;
  align-items: center;
  background-color: #f8f9fa;
  min-height: 100vh;
}

  </style>
</head>
<body>
  <div class="main-layout d-flex">
    <!-- Sidebar -->
    <%- include('adminVertical.ejs') %>

    <!-- Right Side (Content) -->
    <div class="content-area w-100">
      <!-- Navbar -->
      <%- include('adminNav.ejs') %>
=======
  <meta charset="UTF-8">
  <title>View Menu</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Font Awesome v6 -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" integrity="sha512-Evv84Mr4kqVGRNSgIGL/F/aIDqQb7xQ2vcrdIwxfjThSH8CSR7PBEakCr51Ck+w+/U6swU2Im1vVX0SVk9ABhg==" crossorigin="anonymous" referrerpolicy="no-referrer" />

  <!-- Custom CSS -->
  <link rel="stylesheet" href="/css/adminVertical.css">
  <link rel="stylesheet" href="/css/adminNav.css">
   <link rel="stylesheet" href="/css/addmenu.css">

  <style>
    body {
      background-image: url("/images/black.jpg");
      background-size: cover;
      background-repeat: no-repeat;
    }

    .main-layout {
      display: flex;
      width: 100%;
    }

    .adminVertical {
      width: 300px;
      position: fixed;
      left: 0;
      top: 0;
      bottom: 0;
      z-index: 1000;
    }

    .content-area {
      margin-left: 300px;
      width: calc(100% - 300px);
    }

    .view-menu-container {
      padding: 100px 20px 40px;
      display: flex;
      flex-direction: column;
      align-items: center;
      min-height: 100vh;
    }

    .view-card {
      width: 850px;
      background-color: #ffffff;
      box-shadow: 0 0 15px rgba(0,0,0,0.2);
      border-radius: 10px;
      overflow: hidden;
     
    }

    .view-menu-title {
      background: linear-gradient(135deg, #4b0082, #ff7e5f);
      color: white;
      padding: 12px 25px;
     
      font-size: 24px;
      font-weight: bold;
      text-align: center;
      margin-bottom: 20px;
    }

    .search-box {
      margin-bottom: 20px;
    }

    .view-menu-table th {
      background-color: #f4f4f4;
      text-align: center;
      font-weight: bold;
      padding: 10px;
    }

    .view-menu-table td {
      text-align: center;
      vertical-align: middle;
      padding: 10px;
    }

    .view-menu-actions a {
      margin: 0 6px;
      text-decoration: none;
      font-size: 1.3rem;
    }

    .view-menu-actions a.edit {
      color: #0d6efd;
    }

    .view-menu-actions a.delete {
      color: #dc3545;
    }

    .alert {
      width: 850px;
    }
  </style>
</head>
<body>

  <div class="main-layout d-flex">
    <!-- Sidebar -->
    <%- include('adminVertical.ejs') %>

    <!-- Right Side -->
    <div class="content-area w-100">
      <!-- Navbar -->
      <%- include('adminNav.ejs') %>

      <!-- Main Content -->
      <div class="view-menu-container">

        <% if (typeof msg !== 'undefined' && msg) { %>
          <div class="alert alert-success text-center" role="alert">
            <i class="fa-regular fa-face-smile me-2 text-success"></i> <%= msg %>
          </div>
        <% } %>

        <!-- Card container for full table section -->
        <div class="view-card">

          <h2 class="view-menu-title"><i class="fa-solid fa-list me-2"></i>View Menus</h2>

          <!-- Search Box -->
          <div class="search-box">
            <input type="text" name="search" class="form-control" placeholder="🔍 Search menus..." onkeydown="ajaxImpl(this.value)">
          </div>

          <!-- Table -->
          <table class="table table-striped view-menu-table">
            <thead>
              <tr>
                <th>ID</th>
                <th>Menu Name</th>
                <th>Category</th>
                <th>Price</th>
                <th>Description</th>
                <th>Actions</th>
              </tr>
            </thead>
            <tbody>
              <% if (menus.length === 0) { %>
                <tr>
                  <td colspan="6" class="text-muted py-4">No menu items found.</td>
                </tr>
              <% } else { %>
                <% menus.forEach((menu) => { %>
                  <tr>
                    <td><%= menu.id %></td>
                    <td><%= menu.name %></td>
                    <td><%= menu.category %></td>
                    <td>₹<%= menu.price %></td>
                    <td><%= menu.description %></td>
                    <td class="view-menu-actions">
                      <a href="/admin/editmenu/<%= menu.id %>" class="edit" title="Update">
                        <i class="fa-solid fa-pen-to-square"></i>
                      </a>
                      <a href="/admin/deletemenu/<%= menu.id %>" class="delete" title="Delete" onclick="return confirm('Delete this menu?')">
                        <i class="fa-solid fa-trash"></i>
                      </a>
                    </td>
                  </tr>
                <% }) %>
              <% } %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>

  <!-- Bootstrap Bundle JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
>>>>>>> aa49704 (i have added new ejs file also change in src)

      <!-- Main Content -->
      <div class="view-menu-container">
        <h2 class="view-menu-title">View Menus</h2>
        <table class="view-menu-table">
          <thead>
            <tr>
              <th>ID</th>
              <th>Menu Name</th>
              <th>Category</th>
              <th>Price</th>
              <th>Description</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>
            <% menus.forEach((menu) => { %>
              <tr>
                <td><%= menu.id %></td>
                <td><%= menu.name %></td>
                <td><%= menu.category %></td>
                <td>₹<%= menu.price %></td>
                <td><%= menu.description %></td>
                <td class="view-menu-actions">
                  <a href="/admin/editmenu/<%= menu.id %>">Edit</a>
                  <a href="/admin/deletemenu/<%= menu.id %>" onclick="return confirm('Delete this menu?')">Delete</a>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>


</html>